telethon==1.36.0
cryptg==0.4.0

# yt-dlp with the [default] extra (subtitle/format processing) and
# [curl-cffi] (real Chrome/Firefox/Safari TLS-fingerprint impersonation).
yt-dlp[default,curl-cffi]>=2026.8.19

# YouTube Proof-of-Origin token provider: paired with Deno in the Dockerfile
# to generate tokens for 720p/1080p/4K YouTube DASH streams automatically.
bgutil-ytdlp-pot-provider==1.3.1

aiosqlite==0.20.0
aiohttp==3.10.10
python-dotenv==1.0.1

# Embeds ID3 tags (title/artist/cover) for SoundCloud tracks.
mutagen==1.47.0
